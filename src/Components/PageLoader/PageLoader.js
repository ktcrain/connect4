import React from "react";
import Loader from "react-loader-spinner";

function PageLoader() {
  return <Loader type="TailSpin" color="#fff" height={200} width={200} />;
}

export default PageLoader;
